
To-Do
=====

* Write a Set-Cookie parser
  * This includes a date and time parser

* Implement the domain-match algorithm

* Define the type of a cookie-in-a-jar

* Implement the receiving algorithm
  * The rules don't need to see all the cookies at once, so just take one at a time

* Implement send and sendHeaders

* Use gnuidn to convert the binary data to text to provide IDNA compatibility

